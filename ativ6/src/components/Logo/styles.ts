import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logo: {
    borderWidth: 4,
    width: 150,
    height: 150,
  },

  text: {
    color: '#FFFFFF',
    fontSize: 25,
    paddingTop: 8,
    paddingBottom: 100,
  }
});